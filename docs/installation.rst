============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install python_boilerplate
    $ pip install python_boilerplate

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python_boilerplate
    $ pip install python_boilerplate
